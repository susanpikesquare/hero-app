# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# Office lock files — Word/Excel create ~$filename when a doc is open,
# and a stale one in the working tree fails fastlane's ensure_git_status_clean.
~$*

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

example

# Native iOS — we commit the Xcode project (ios/HomeHero.xcodeproj,
# ios/HomeHero.xcworkspace, ios/Podfile etc.) so `git pull` is enough
# to be ready to build. But we gitignore the dependency + build output
# directories that get regenerated locally.
/ios/Pods/
/ios/build/
/ios/DerivedData/
/ios/*.xcodeproj/xcuserdata/
/ios/*.xcworkspace/xcuserdata/

# Android still regenerated by `expo prebuild` per change.
/android

# Vercel
.vercel
dist

# Claude Code (local-only; share settings.json + launch.json across the team)
.claude/settings.local.json

# Fastlane (local iOS release pipeline)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output
fastlane/*.cer
fastlane/*.p12

# Local archive output from `fastlane build` / `fastlane beta`
build/*.ipa
build/*.xcarchive
build/*.app.dSYM.zip

# Local test photos saved into assets/images while QA-testing chores.
# Real app images are committed individually; these scratch shots are not.
/assets/images/9 year old boy bed made.png
/assets/images/boys bed made-9-kid attempt.png
/assets/images/boys bed made-9.png
